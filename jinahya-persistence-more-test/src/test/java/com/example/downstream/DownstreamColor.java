package com.example.downstream;

import com.github.jinahya.persistence.more.colormodel.__MappedRgba;

/** A colour a downstream maps, extending the mapped superclass as its own entity would. */
public class DownstreamColor extends __MappedRgba {

}
