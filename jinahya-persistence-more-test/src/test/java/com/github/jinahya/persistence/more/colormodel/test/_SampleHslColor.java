package com.github.jinahya.persistence.more.colormodel.test;

import com.github.jinahya.persistence.more.colormodel.__MappedHsl;

/**
 * A color sample whose first component is a hue, which is the component the generic assertions have to be careful
 * with: it is an angle, so it is circular rather than merely bounded.
 */
class _SampleHslColor extends __MappedHsl {

}
